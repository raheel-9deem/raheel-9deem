# Generated profile assets

`github-contributions.svg` is generated by `scripts/generate_activity.py`.

It always represents exactly the rolling previous 365 calendar days.
The workflow runs daily, so when a new date arrives the oldest date
automatically drops out of the visible calendar.

Do not manually edit the generated SVG.
